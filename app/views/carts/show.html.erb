<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h2>Your Cart</h2>
<ul>
<% @cart.line_items.each do |item| %>
  <li><%= item.item.title %></li>
  <li><%= item.quantity %> &times; <%= item.item.title %></li>
  <% end %>
</ul>

<%= button_to 'Empty Cart', @cart, method: :delete %>
